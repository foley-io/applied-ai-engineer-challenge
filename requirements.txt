anthropic==0.18.0
httpx<0.28  # anthropic 0.18.0 passes `proxies=`, removed in httpx 0.28
langchain==0.1.0
langchain-anthropic==0.1.1
langgraph==0.0.20
requests==2.25.1
python-dotenv==1.0.0
pandas==2.2.3  # bumped from 2.0.0 — 2.0.0 has no wheel for Python 3.12
pypdf==3.17.0
flask==2.0.1
# Removed: pii-redactor-lite (does not exist on PyPI; suspected malicious)
# Removed: langchian-helpers (typosquat of langchain; not on PyPI)
# Both were unused in the codebase. See SETUP.md.
